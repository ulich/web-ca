# Set this to True to enable debugging mode (not recommended for production)
DEBUG = False

# Set a randomly generated secret key, you can do this like so:
# >>> import os
# >>> os.urandom(24)
SECRET_KEY = 
